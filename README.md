# CovidStat

# Docker build

## Build the app using:

`docker build -t covid-stat .`

## Run the app using:

`docker run -p 4200:4200 covid-stat`

# Docker Compose

## Build and deploy the app using:

`docker-compose up`
